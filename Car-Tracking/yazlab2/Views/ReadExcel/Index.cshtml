@using yazlab2.Models
@model IEnumerable<ReadExcelModel>
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>

    @if (Model.Count() > 0)
    {
        <hr />
        <table cellpadding="0" cellspacing="0" border="1">
            <tr>
                <th>id</th>
                <th>tarih</th>
                <th>konum</th>
                <th>konum2</th>
            </tr>
            @foreach (ReadExcelModel user in Model)
            {
                <tr>
                    <td>@user.CarId</td>
                    <td>@user.Time</td>
                    <td>@user.Lang</td>
                    <td>@user.Lat</td>
                </tr>
            }
        </table>
    }

    <button type="button" onclick="location.href='@Url.Action("Index","Message")'" class="btn btn-primary" style="float:right">Message Broker</button>
</body>
</html>